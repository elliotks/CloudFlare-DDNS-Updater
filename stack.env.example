# This is a token you generate in the Cloudflare dashboard under "My Profile" > "API Tokens".
# It provides authenticated access to the API without exposing your main account password.
# Example: 123456789abcdef123456789abcdef12345678.
CLOUDFLARE_API_TOKEN=

# The fully qualified domain name (FQDN) that you're updating with your dynamic IP.
# This should match one of the DNS records in your Cloudflare account.
# Example: home.example.com.
CLOUDFLARE_DNS_NAME=

# The IP version to use for updating the DNS record.
# Valid values: ipv4, ipv6
# Default value: ipv4
CLOUDFLARE_IP_VERSION=ipv4

# The URL to query for retrieving the public IPv4 address.
# Default value: https://api.ipify.org
CLOUDFLARE_IPV4_URL=

# The URL to query for retrieving the public IPv6 address.
# Default value: https://api64.ipify.org
CLOUDFLARE_IPV6_URL=

# How often to update the Cloudflare DNS record in seconds.
# The minimum allowed interval is 60 seconds.
# Default value: 600 (10 minutes).
CLOUDFLARE_DNS_UPDATE_INTERVAL=

# The Time to Live (TTL) value for the updated DNS record in seconds.
# Default value: 120 (2 minutes).
CLOUDFLARE_DNS_TTL=

# Automatically create the DNS record if it doesn't exist.
# Valid values: true, false
# Default value: false
CLOUDFLARE_AUTO_CREATE_DNS=